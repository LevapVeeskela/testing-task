import { Location } from 'react-router-dom';

export const ROUTES = {
	default: '/'
}

export const LOCATION_DEFAULT: Location = {
	pathname: ROUTES.default,
	search: '',
	hash: '',
	state: {},
	key: ''
};
